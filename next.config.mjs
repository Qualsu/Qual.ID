/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    basePath: "/QualID",
    reactStrictMode: true,
    images: {
        unoptimized: false
    }
};

export default nextConfig;
